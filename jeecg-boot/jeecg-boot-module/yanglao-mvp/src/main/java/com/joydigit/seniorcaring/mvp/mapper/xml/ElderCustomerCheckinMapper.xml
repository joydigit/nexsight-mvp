<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.joydigit.seniorcaring.mvp.mapper.ElderCustomerCheckinMapper">

    <select id="getLastInfoByCustomerId" resultType="com.joydigit.seniorcaring.mvp.entity.ElderCustomerCheckin">
        SELECT
            c.*,
            b.bed_no,
            r.room_no,
            r.floor_id,
            f.floor_name,
            r.building_id,
            eb.building_name
        FROM
            elder_customer_checkin c
                LEFT JOIN elder_bed b ON b.id = c.bed_id
                LEFT JOIN elder_room r ON r.id = c.room_id
                LEFT JOIN elder_floor f ON f.id = r.floor_id
                LEFT JOIN elder_building eb ON eb.id = r.building_id
        WHERE
            c.del_flag = 0
          AND c.customer_id = #{customerId}
        ORDER BY
            c.create_time DESC
            limit 1
    </select>
</mapper>